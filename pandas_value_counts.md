# pandas\_value\_counts

See exactly how it counts unique values — step by step.

## Your DataFrame column

```python
df["fruit"].value_counts()
```

## `value_counts()` result

Start

Reset

Click Start to begin

**What is `value_counts()`?** It counts how many times each unique value appears in a column. Hit **Start** to see it in action.
